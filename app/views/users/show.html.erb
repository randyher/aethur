<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body class = "user-body">
        <div class="profile-image-container">  <%= image_tag(@user.avatar, class: "profile-image")%>
        </div>
        <h1><%="Welcome #{@user.username}"%></h1>
            <div class = "container-body">
        <hr>

        <h2>My Videos</h2>
        <%=link_to "New Video", new_user_video_path(@user)%>
        <hr>
        <div class="album text-muted">
            <div class="container">

            <div class="row">
                <%@videos.each do |video| %>
                 <a href = "<%=user_video_path(:user_id,video)%>">
                <div class="card">
                    <%= image_tag(video.thumbnail, class: "card-image") %>
                    <p class="card-text">This is a wider card with supporting text below as a natural lead-in to additional content. This content is a little bit longer.</p>
                </div>
            </a>
                <%end %>
            </div>
        </div>
        </div>
        <hr>
        <h2>My Posts</h2>
        <%=link_to "New Post", new_user_blog_path(@user)%>
        <hr>
        <div class="album text-muted">
            <div class="container">

            <div class="row">
                <%@blogs.each do |blog| %>
                 <a href = "<%=user_blog_path(:user_id,blog)%>">
                <div class="card">
                    <%= image_tag(blog.thumbnail, class: "card-image") %>
                    <p class="card-text">This is a wider card with supporting text below as a natural lead-in to additional content. This content is a little bit longer.</p>
                </div>
            </a>
                <%end %>
            </div>
        </div>
        </div>
    </div>

    </body>
</html>
